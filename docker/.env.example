# PostgreSQL local development credentials.
# Copy this file to `.env` (same folder) and adjust values if needed.
# `.env` is gitignored — never commit real credentials.

PGUSER=eventory
PGPASSWORD=changeme
PGNAME=eventory_dev
PGPORT=5432